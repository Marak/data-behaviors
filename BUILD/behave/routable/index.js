// the routable behavior is used for listening to 

$(options.selector).ready(function(e){
  debug.log('im ready and routable'); 
  
  
  var state = location.hash;
  //machine.enter( state.replace('#/',''), context );
  
  debug.log(location.hash);
  
});